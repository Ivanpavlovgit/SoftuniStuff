package Person;

public class Child extends Person {
    //must reuse constructor from super class ( person -with small letters)
    public Child (String name,int age) {
        super (name,age);
    }
}
